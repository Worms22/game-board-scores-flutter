name: flutter_structure
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.18.4 <3.0.0'


dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  crow: 1.1.0
  cupertino_icons: ^1.0.6
  device_info_plus: ^8.2.2 #flutter_evironment, device_info_plus
  flutter_secure_storage: ^8.1.0 #device_info_plus
  google_fonts: ^6.1.0
  dio: ^4.0.6 #duckma_client, connectivity_plus
  intl: ^0.18.1
  json_annotation: ^4.8.1
  path_provider: ^2.1.1
  flutter_environment: ^0.0.2
  flutter_dotenv: ^5.1.0
  shared_preferences: ^2.2.1
  yaml: ^3.1.1
  get: ^4.6.6
  duckma_client: ^0.0.2
  connectivity_plus: ^3.0.2
  flutter_native_splash: ^2.2.17
  flutter_svg: ^1.1.6
  carousel_slider: ^4.2.1
  url_launcher: ^6.1.5
  step_progress_indicator: ^1.0.2
  package_info_plus: ^4.1.0
  keycloak_flutter: ^0.0.21


dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.6
  json_serializable: ^6.3.1
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  generate: true

  assets:
    - .env
    - pubspec.yaml
    - assets/images/